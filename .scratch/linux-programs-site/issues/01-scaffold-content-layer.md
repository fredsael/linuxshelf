# 01: Project Scaffold + Content Layer

**What to build:** A working Astro project with a validated YAML content loader and two sample programs. `npm run build` succeeds and outputs static pages.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] Astro project initialized with TypeScript
- [x] YAML schema defined (all fields from spec)
- [x] Content loader reads `content/programs/*.yaml`, validates, returns typed objects
- [x] Build fails on invalid YAML with descriptive error
- [x] Two sample program YAML files (e.g., neovim, alacritty) with all fields populated
- [x] Category derivation utility works (returns unique categories with counts)
- [x] Base layout with monochrome design, Inter + monospace fonts
